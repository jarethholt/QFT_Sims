[project]
name = "qft-sims"
version = "0.1.0"
description = "Animations for the QFT video"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.0",
    "numpy>=2.2.2",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
    "ipython>=8.32.0",
    "mypy>=1.15.0",
    "ruff>=0.9.6",
]
